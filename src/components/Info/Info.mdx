```typescript
type Props = {
  title: string;
  content: string;
};

export function Info({ title, content }: Props) {
  return (
    <div>
      <p>{title}</p>
      <p>{content}</p>
    </div>
  );
}
```
